{
  "name": "critical-error",
  "version": "1.2.1",
  "description": "A simple interface to send error messages to an SNS topic.",
  "main": "index.js",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Crosby <james@coggle.it>",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-sns": "^3.282.0"
  }
}
